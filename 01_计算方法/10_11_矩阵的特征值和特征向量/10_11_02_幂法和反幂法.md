

# 1 计算按模最大的特征值和特征向量 

完备的特征向量组: n阶矩阵 正好有n个线性无关的特征向量组 

![](image/Pasted%20image%2020260409184934.png)
特征值 lambda_1  是所有特征值的中绝对值最大的那个 , 叫做按模最大特征值

---

因为 x_i 为  A 的特征向量, 所以 A^m 乘上 x_i  等于  lambda^m 乘上 x_i 

![](image/Pasted%20image%2020260409185447.png)

特征值 lambda_1  是所有特征值的中绝对值最大的那个 , 叫做按模最大特征值
![](image/Pasted%20image%2020260409185752.png)


![](image/Pasted%20image%2020260409185903.png)

==可以得到相邻两个迭代的 同一个特征向量之间, 存在关系 x^(m+1) 等于  lambda_1 乘上  x^(m) ==
特征值 lambda_1  是所有特征值的中绝对值最大的那个 , 叫做按模最大特征值

---

分量: 

m 趋向于无穷大的时候,  theta 趋向于0 , theta 的定义 见上面第二张图 

![](image/Pasted%20image%2020260409190326.png)



相邻两个迭代向量的相同分量的比值收敛到 按模最大特征值 , 
![](image/Pasted%20image%2020260409191002.png)

----


如果 lambda_1 > 1 , 则不停迭代后, 这个特征向量趋向于 无穷大, 我们不希望看到
如果 lambda_1 < 1 , 则不停迭代后, 这个特征向量趋向于 0, 我们不希望看到
如果 lambda_1 = 1 , 我们就不需要迭代 特征向量x 了,  x 在迭代中始终保持不变,  那么迭代就没有意义了 
上面是三头堵,  按 lambda_1 的模去迭代 特征向量 没有意义了  

![](image/Pasted%20image%2020260409191144.png)

![](image/Pasted%20image%2020260409191249.png)


----
## 1.1 

因为 上面是三头堵,  按 lambda_1 的模去迭代 特征向量 没有意义了  , 我们需要优化迭代公式 , 使用到规范化 regularization 


max(Z) 表示  矩阵Z 中绝对值最大的元素, 而不是最大的特征值 
![](image/Pasted%20image%2020260409191715.png)

---

结论1: 再看 x^m 
![](image/Pasted%20image%2020260409192319.png)

x_1: 对于按模最大的特征值 对应的特征向量 
max(x_1):  对于按模最大的特征值 对应的特征向量   中 最对只最大的元素 , 为一个常数 
==结论说明 x^(m) 趋于 按模最大的特征值 对应的 特征向量 ==

按模最大的特征值 是什么 
![](image/Pasted%20image%2020260409184934.png)


----

结论2: 
再看 max(Z^(m))
![](image/Pasted%20image%2020260409192630.png)


得到 lambda^(m) =  max (Z^(m)) 表示 lambda_1 第m次的近似值 

### 1.1.1 总结论 

下面的 lambda 为 Z^(m) 最大的分量 的特征值 

![](image/Pasted%20image%2020260409192959.png)


---

收敛速度 

![](image/Pasted%20image%2020260409193406.png)

lambda_i / lambda_1 的商 越小, 收敛的越快
lambda_i  在 n 个 lambda_i  中 除了 i=1 外,  lambda_2 的模最大

则 lambda_2 / lambda_1  的值越小, 收敛速度越快 




## 1.2 收敛加速方法: Reyleigh 商